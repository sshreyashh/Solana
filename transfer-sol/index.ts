import { Connection, Keypair, PublicKey } from "@solana/web3.js";

export const transferSol = (sender : Keypair , reciever : PublicKey , amount : number) => {

    const senderkey = conn.getAccountInfo ()

    const conn = new Connection ("")

}